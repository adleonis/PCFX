<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.pcfx.node.androidn1">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service for atomization job -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Scheduling permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="false"
        android:debuggable="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Main dashboard activity -->
        <activity
            android:name="org.pcfx.node.androidn1.ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- PDV settings activity -->
        <activity
            android:name="org.pcfx.node.androidn1.ui.SettingsActivity"
            android:exported="false"
            android:label="PDV Settings" />

        <!-- Schedule configuration activity -->
        <activity
            android:name="org.pcfx.node.androidn1.ui.ScheduleActivity"
            android:exported="false"
            android:label="Schedule Settings" />

        <!-- Atomization worker service (implicit start) -->
        <service
            android:name="org.pcfx.node.androidn1.work.AtomizeWorker"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Daily alarm receiver for scheduled runs -->
        <receiver
            android:name="org.pcfx.node.androidn1.work.DailyAlarmReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="org.pcfx.node.androidn1.ATOMIZE_DAILY" />
            </intent-filter>
        </receiver>

        <!-- Boot completed receiver to reschedule alarms -->
        <receiver
            android:name="org.pcfx.node.androidn1.work.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
